delete
docker stop $(docker ps -aq)
docker rm $(docker ps -aq)
docker rmi -f $(docker images -q)
docker system prune -a -f
docker system prune -a
docker volume prune -f
docker compose down --volumes --rmi all --remove-orphans

docker compose down       # stop & remove old containers
docker compose up -d --build   # rebuild images and start fresh containers in background

docker system prune


Docker Container Cleanup – Key Commands

Reduce waste. Improve stability. Optimize spend.

1. Remove Stopped Containers

Objective: Eliminate inactive workloads

docker rm <container>
docker container prune


Impact: Frees resources and reduces operational clutter.

2. Image Cleanup

Objective: Control storage growth and image sprawl

docker rmi <image>
docker image prune
docker image prune -a


Impact: Optimizes disk usage and enforces image discipline.

3. Volume Cleanup

Objective: Remove unused persistent storage

docker volume ls
docker volume prune


Impact: Prevents silent cost accumulation.

4. Network Cleanup

Objective: Remove unused network artifacts

docker network ls
docker network prune


Impact: Simplifies connectivity and reduces misconfiguration risk.

5. Full System Optimization

Objective: End-to-end environment hygiene

docker system df
docker system prune
docker system prune -a --volumes

