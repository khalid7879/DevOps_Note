🔐 Goal

✔ Login from Server A → Server B without password
✔ Login from Server B → Server A without password

STEP 1️⃣ Generate SSH key on Server A

Login to 114.130.69.249 using password one last time:

ssh root@114.130.69.249


Generate SSH key:

ssh-keygen -t rsa -b 4096


👉 Press Enter for all prompts
(Key will be saved in /root/.ssh/id_rsa)

STEP 2️⃣ Copy key from Server A → Server B

From Server A:

ssh-copy-id root@114.130.69.223


🔑 Enter password once:

noc@dev@


✅ Now Server A can login to Server B without password.

Test it:

ssh root@114.130.69.223

STEP 3️⃣ Generate SSH key on Server B

Login to 114.130.69.223:

ssh root@114.130.69.223


Generate key:

ssh-keygen -t rsa -b 4096


Press Enter for all prompts.

STEP 4️⃣ Copy key from Server B → Server A

From Server B:

ssh-copy-id root@114.130.69.249


Enter password:

noc@dev@


Test it:

ssh root@114.130.69.249

✅ DONE — Passwordless SSH Enabled 🎉

Now you can:

ssh root@114.130.69.249
ssh root@114.130.69.223


👉 No password required

🔒 (Optional but Recommended) Disable Root Password Login

After confirming everything works:

Edit SSH config on both servers:

nano /etc/ssh/sshd_config


Change:

PasswordAuthentication no
PermitRootLogin prohibit-password


Restart SSH:

systemctl restart sshd


⚠️ Do this only after confirming SSH keys work